"""Synthetic marketplace data generation for the M2 build.

Generators are imported from ``src.data.generate_marketplace`` directly. The
package intentionally has no eager imports so ``python -m
src.data.generate_marketplace`` does not execute the module twice.
"""
